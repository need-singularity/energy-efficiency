__pycache__/
*.pyc
.env
data/
# atlas canonical symlinks — tracked exception (SSOT, raw#10 proof-carrying, anima 패턴)
# 실제 symlink 는 n6shared/atlas.n6, n6shared/atlas.signals.n6 (data/ 미사용)
# → ../../data/n6/atlas{.signals,}.n6 (canonical target: ~/core/data/n6/)
!/data/n6/atlas.n6
!/data/n6/atlas.signals.n6
models/
.claude/*
!.claude/settings.json
!.claude/hooks/
.discovery-cache
# Rust 빌드 아티팩트
**/target/
.growth/

# 레거시 origins (추적 제거 완료, 재추가 방지)
bridge/origins/nexus-legacy/
bridge/origins/nexus-dashboard-legacy/

# 레거시 심링크 (TECS-L 폐기 후 잔류, 무시)
calc
.shared
model_utils.py
tecsrs

# Claude Code memory (account-shared, local only)
.claude-memory/

# macOS
.DS_Store
build/

# Lean4 build artifacts (v3 M3)
lean4-n6/.lake/
lean4-n6/lake-manifest.json

# Python virtual envs + egg-info (venv 폐기 재추적 방지, ROI#13)
venv/
.venv-btAI1/
*.egg-info/

# Transient hook command state (per-invocation)
.hook-commands/

# Infra state symlink → ~/core/nexus/infra_state.json (out-of-repo SSOT)
infra_state.json

# Working-tree noise (2026-04-29 cycle 29 closure)
# state/markers/*.marker — accumulated test/probe markers (60+ prefixes observed
#   2026-04-29: _*, alien_grade_audit_*, anon_*, probe*_*, debug_*, dump_*, etc.)
#   Tracked atlas_absorb_*.marker files (SSOT) remain tracked — gitignore does
#   NOT untrack files already in the index. Future untracked markers ignored.
# scratch/ — local exploratory workspace, never tracked
# lean4-n6/state/ — lean4 transient state (lake build derivative)
state/markers/*.marker
scratch/
lean4-n6/state/

# .workspace machine-local SSOT (mk2 schema, NEVER github push) — added 2026-05-02
.workspace
.workspace.*
.workspace.lock

# Lean4 compile output
*.olean
