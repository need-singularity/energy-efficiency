## Scope (English)

This file belongs to the `chip-hbm` domain (axis: `compute`) in the n6-architecture repository.
Canonical documentation: `domains/compute/chip-hbm/chip-hbm.md`.
Registry: `domains/_index.json`.

# chip-hbm — compute 도메인

files:  chip-hbm.md (15섹션, 검증코드 인라인)
blowup: blowup.hexa <d> 3

parent: ../../CLAUDE.md → "compute"
