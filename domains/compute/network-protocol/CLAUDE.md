## Scope (English)

This file belongs to the `network-protocol` domain (axis: `compute`) in the n6-architecture repository.
Canonical documentation: `domains/compute/network-protocol/network-protocol.md`.
Registry: `domains/_index.json`.

# network-protocol — compute 도메인

files:  network-protocol.md (15섹션, 검증코드 인라인)
blowup: blowup.hexa <d> 3

parent: ../../CLAUDE.md → "compute"
