## Scope (English)

This file belongs to the `compiler-os` domain (axis: `compute`) in the n6-architecture repository.
Canonical documentation: `domains/compute/compiler-os/compiler-os.md`.
Registry: `domains/_index.json`.

# compiler-os — compute 도메인

files:  compiler-os.md (15섹션, 검증코드 인라인)
blowup: blowup.hexa <d> 3

parent: ../../CLAUDE.md → "compute"
